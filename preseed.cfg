# Ce preseed ne repond QUE aux questions techniques sans interet (miroir,
# paquets, chargeur de demarrage). Toutes les questions "classiques" restent
# posees normalement : langue, clavier, fuseau horaire, partitionnement,
# nom de machine. Root login SSH est active automatiquement a la fin.

d-i console-setup/ask_detect boolean true

d-i clock-setup/ntp boolean true

d-i passwd/root-login boolean true
d-i passwd/make-user boolean false

tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string curl rsync git
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

d-i preseed/late_command string \
    in-target sed -i 's/^#PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config; \
    in-target chage -d 0 root
