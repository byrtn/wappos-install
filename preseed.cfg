d-i debian-installer/locale string fr_FR.UTF-8
d-i keyboard-configuration/xkb-keymap select fr

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string wappos
d-i netcfg/get_domain string local
d-i netcfg/hostname string wappos

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

d-i clock-setup/utc boolean true
d-i time/zone string Europe/Paris
d-i clock-setup/ntp boolean true

d-i passwd/root-login boolean true
d-i passwd/make-user boolean false
d-i passwd/root-password password wapposinstall
d-i passwd/root-password-again password wapposinstall

d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string curl rsync git
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

d-i debian-installer/exit/halt boolean true
d-i finish-install/reboot_in_progress note

d-i preseed/late_command string \
    in-target sed -i 's/^#PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config; \
    in-target chage -d 0 root; \
    echo "" > /dev/console; \
    echo "========================================================" > /dev/console; \
    echo "  INSTALLATION DEBIAN TERMINEE AVEC SUCCES" > /dev/console; \
    echo "" > /dev/console; \
    echo "  La machine va maintenant s'ETEINDRE toute seule." > /dev/console; \
    echo "  C'est normal, ce n'est PAS une erreur." > /dev/console; \
    echo "" > /dev/console; \
    echo "  Une fois eteinte : retirez le support d'installation" > /dev/console; \
    echo "  (CD/USB), puis redemarrez la machine sur son disque dur." > /dev/console; \
    echo "========================================================" > /dev/console; \
    sleep 5
